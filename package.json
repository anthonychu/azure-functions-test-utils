{
  "name": "20220118-func-testing",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "prestart": "npm run build",
    "start": "func start",
    "test": "npm run prestart && jest --runInBand --verbose"
  },
  "dependencies": {
    "durable-functions": "^1.5.4"
  },
  "devDependencies": {
    "@anthonychu/azure-functions-test-utils": "file:lib"
  }
}
